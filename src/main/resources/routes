# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /json                           microservice.HelloWorldController.json
GET     /plaintext                      microservice.HelloWorldController.plaintext
GET     /metrics                        microservice.MetricsController.metrics